{
  "id": "DS-1",
  "name": "clamp-audit-design-from-research",
  "skill": "design",
  "worktree": "C:/Users/pedro/Documents/GymApp-evals/wt-head",
  "ref": "b460257",
  "clean": [".rpiv/artifacts/designs"],
  "seed": [
    { "from": "../_fixtures/research-load-cap-clamp-audit.md", "to": ".rpiv/artifacts/research/2026-07-08_13-10-00_load-cap-clamp-audit.md" }
  ],
  "artifact_glob": ".rpiv/artifacts/designs/*.md",
  "prompt": "/rpiv:design .rpiv/artifacts/research/2026-07-08_13-10-00_load-cap-clamp-audit.md (Headless non-interactive run: at any approval checkpoint, per-slice micro-checkpoint, or question, ALWAYS pick the recommended option and continue; never wait for user input. Proceed all the way through slice generation and finalization.)",
  "ground_truth_note": "Design consumes the pinned research fixture (feature: in-memory clamp audit trail on the validation seam). A correct design must: reuse load-cap.ts predicates (not re-derive the epsilon), hook the recorder at validation.ts, decompose into vertical slices each with a #### Test Contract:, and finalize to status: ready with a content_hash stamp. New files it creates (load-cap-audit/*) will NOT resolve at HEAD by design — grading uses validate_artifact + required-sections + recall on the EXISTING grounding files (load-cap.ts, validation.ts), NOT citation resolvability.",
  "checks": {
    "validate_artifact": true,
    "required_sections": [
      "^## Architecture",
      "^## Slices",
      "^### Slice 1",
      "#### Test Contract:",
      "^## Decisions",
      "^## File Map"
    ],
    "recall": [
      {
        "id": "reuses-load-cap-owner",
        "files": [{ "path": "lib/generation/load-cap.ts" }],
        "min_files": 1,
        "note": "design grounds on and reuses the load-cap owner module (not a re-derived epsilon)"
      },
      {
        "id": "hooks-validation-seam",
        "files": [{ "path": "lib/generation/adaptive-context/validation.ts" }],
        "min_files": 1,
        "note": "design hooks the clamp recorder into the validation seam identified in research"
      }
    ],
    "frontmatter": {
      "present": ["status", "parent"]
    }
  }
}
