{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": { "configFile": "vitest.config.mjs" },
  "coverageAnalysis": "perTest",
  "reporters": ["html", "clear-text", "progress"],
  "mutate": ["skills/_shared/store.mjs"],
  "thresholds": { "high": 90, "low": 80, "break": null },
  "incrementalFile": "reports/stryker-incremental.json",
  "htmlReporter": { "fileName": "reports/mutation/store.html" },
  "_status": "Action Item #2 scaffold. Runs green only AFTER Action Item #6 converts skills/_shared/store.test.mjs to vitest (Stryker's vitest-runner needs vitest tests to score mutants). mutate is scoped to the 90+ target store.mjs; `break` stays null until #6 so a premature run does not fail a build. mutate:plan uses --incremental."
}
